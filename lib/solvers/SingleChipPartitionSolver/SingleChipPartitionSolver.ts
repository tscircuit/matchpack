/**
 * Creates a single partition for a single chip.
 * Handles the partitioning logic for individual chips within the overall layout.
 */

import { BaseSolver } from "../BaseSolver"

export class SingleChipPartitionSolver extends BaseSolver {
  constructor() {
    super()
  }

  override _step() {
    // TODO
  }

  override visualize() {
    return super.visualize()
  }
}
